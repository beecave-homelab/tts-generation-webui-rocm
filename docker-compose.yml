services:
  tts-generation-webui:
    build:
      context: .
      dockerfile: Dockerfile-amd
    image: tts-generation-webui:rocm-6.1.1-pytorch-2.1.2
    restart: unless-stopped
    ports:
      - "7860:7860"
      - "3000:3000"
    devices:
      - "/dev/kfd:/dev/kfd"
      - "/dev/dri:/dev/dri"
    stdin_open: true
    tty: true
    cap_add:
      - SYS_PTRACE
    command: "/bin/bash"
    security_opt:
      - seccomp=unconfined
    group_add:
      - video
    ipc: host
    shm_size: 8G